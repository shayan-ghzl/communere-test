import { DaysArrayPipe } from './days-array.pipe';

describe('DaysArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
