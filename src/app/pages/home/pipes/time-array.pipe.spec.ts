import { TimeArrayPipe } from './time-array.pipe';

describe('TimeArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
